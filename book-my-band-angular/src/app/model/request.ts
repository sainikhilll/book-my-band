import { Band } from "./band";
import { Customer } from "./customer";

export class Requests {
    // id: number;
    // public name: string,
    date!: Date;
    hours!: number;
    city!: string;
    status!: string;
    band!: Band;
    customer!: Customer
   }